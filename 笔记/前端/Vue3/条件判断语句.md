## v-if
```ts
	<!-- v-if 链式判断 -->
	<div v-if="type === 'A'">A类内容</div>
	<div v-else-if="type === 'B'">B类内容</div>
	<div v-else>其他内容</div>
```
## v-show
```ts
	<!-- v-show 显示控制 -->
	<div v-show="isVisible">通过display属性控制</div>
```