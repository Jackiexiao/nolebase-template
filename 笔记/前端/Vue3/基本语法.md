```ts
	//引入createApp,用于创建应用
	import { createApp } from 'vue'
		//1. `'vue'`指向node_modules中的Vue npm包
		//2. `createApp`是Vue 3的应用初始化函数
	//引入App根组件
	import App from './App.vue'
	//创建应用并将App根组件挂载到#app
	createApp(App).mount('#app')
```