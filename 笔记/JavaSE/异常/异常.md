---
share: "true"
---
![Pasted image 20251031163604](../../img/Pasted%20image%2020251031163604.png)
![Pasted image 20251031164310](../../img/Pasted%20image%2020251031164310.png)
![Pasted image 20251031164346](../../img/Pasted%20image%2020251031164346.png)

## 编译时异常

<!-- prettier-ignore -->
> [!NOTE] Title
> 	在编译阶段，必须手动处理，否则代码报错

## 运行时异常

<!-- prettier-ignore -->
> [!NOTE] Title
> 	在编译阶段时不需要处理的，时代码运行时出现的异常

![Pasted image 20251031164822](../../img/Pasted%20image%2020251031164822.png)
![Pasted image 20251031165703](../../img/Pasted%20image%2020251031165703.png)

## 异常的作用

![Pasted image 20251031165747](../../img/Pasted%20image%2020251031165747.png)

```Java
Student st = new Student();
st.setAge(55);

public void setAge(int age) {
    if (age < 0 || age > 50) {
        throw new RuntimeException();
    }else {
        this.age = age;
    }
}
```

![Pasted image 20251031190832](../../img/Pasted%20image%2020251031190832.png)
![Pasted image 20251031192358](../../img/Pasted%20image%2020251031192358.png)

```Java
public class ExceptionDemo05 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        GirlFriend gf = new GirlFriend();
        while (true) {
            try {
                System.out.println("请输入姓名:");
                String name = sc.nextLine();
                gf.setName(name);
                System.out.println("请输入年龄:");
                String ageStr = sc.nextLine();
                int age = Integer.parseInt(ageStr);
                gf.setAge(age);
                break;
            } catch (NumberFormatException e) {
                System.out.println("年龄必须是数字");
            }catch (RuntimeException e) {
                System.out.println(e.getMessage());
            }
        }
        System.out.println(gf);
    }
}

public class GirlFriend {
    private String name;
    private int age;
    public void setAge(int age) {
	    if (age < 18 || age > 30) {
	        throw new RuntimeException("年龄必须在18到30之间");
		}
	    this.age = age;
	}
	public void setName(String name) {
	    if (name.length() < 3 || name.length() > 10){
	        throw new RuntimeException("姓名长度必须在3到10之间");
		}
	    this.name = name;
	}
}
```

## 编译时异常必须要在类名后面写 -> thows 异常名称 <-
