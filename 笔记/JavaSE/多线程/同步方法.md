---
share: "true"
---

![](../../img/Pasted%20image%2020251129153823.png)

```Java
public class MultiDemo10 {
    public static void main(String[] args) {
        MyThread08 mt = new MyThread08();
        Thread t1 = new Thread(mt, "窗口1");
        Thread t2 = new Thread(mt, "窗口2");
        Thread t3 = new Thread(mt, "窗口3");
        t1.start();
        t2.start();
        t3.start();
    }
}

public class MyThread08 implements Runnable{
    int ticket = 0;
    @Override
    public void run() {
        while (true){
            synchronized (MyThread08.class){
                if (method()) break;
            }
        }
    }

    private synchronized boolean method() {
        if (ticket < 100){
            System.out.println(Thread.currentThread().getName() +
                    " 卖出了第" + (ticket + 1) + "张票");
            ticket++;
        } else {
            return true;
        }
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }
        return false;
    }
}
```
