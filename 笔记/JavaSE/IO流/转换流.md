---
share: "true"
---

![](../../img/Pasted%20image%2020251110164035.png)
![](../../img/Pasted%20image%2020251110164140.png)

```Java
//JDK11之前（被淘汰了）
public class IODemo23 {
    public static void main(String[] args) throws IOException {
        InputStreamReader isr = new InputStreamReader(
                new FileInputStream("src/javaStudy/MyIO/file.txt"), "GBK");
        int len;
        OutputStreamWriter osw = new OutputStreamWriter(
                new FileOutputStream("src/javaStudy/MyIO/d.txt"), "UTF-8");
        while ((len = isr.read()) != -1) {
            osw.write((char) len);
        }
        osw.close();
        isr.close();
    }
}
```

```Java
//在JDK11及之后
public class IODemo24 {
    public static void main(String[] args) throws IOException {
        FileReader fr = new FileReader(
                "src/javaStudy/MyIO/file.txt", Charset.forName("GBK"));
        int len;
        FileWriter fw = new FileWriter(
                "src/javaStudy/MyIO/d.txt", Charset.forName("UTF-8"));
        while ((len = fr.read()) != -1) {
            fw.write((char) len);
        }
        fw.close();
        fr.close();
    }
}
```

```java
public class IODemo25 {
    public static void main(String[] args) throws IOException {
        InputStreamReader isr = new InputStreamReader(
                new FileInputStream("src/javaStudy/MyIO/file.txt"),"GBK");
        BufferedReader br = new BufferedReader(isr);
        String line = br.readLine();
        System.out.println(line);
        br.close();
    }
}
```

![](../../img/Pasted%20image%2020251110172048.png)
